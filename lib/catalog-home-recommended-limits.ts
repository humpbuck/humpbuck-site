/** Shared client/server caps for homepage product arrange. */
/** Max watches in each homepage category carousel (ANA-DIGI / Digital / Analog / Automatic). */
export const MAX_HOME_RECOMMENDED_PER_CATEGORY = 12;
/** Four homepage category carousels × per-category cap. */
export const MAX_HOME_RECOMMENDED = MAX_HOME_RECOMMENDED_PER_CATEGORY * 4;
export const MAX_HOME_FEATURED = 12;
