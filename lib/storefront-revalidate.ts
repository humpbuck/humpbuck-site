/** ISR window for catalog `unstable_cache` entries. Page `export const revalidate` must use the same literal (300) — Next.js rejects imported segment config at build time. */
export const STOREFRONT_ISR_SECONDS = 300;
