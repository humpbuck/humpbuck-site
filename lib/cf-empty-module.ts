/** Stub module for Cloudflare Worker builds — excludes local-only deps from the bundle. */
export default {};

export function Analytics() {
  return null;
}

export function SpeedInsights() {
  return null;
}
